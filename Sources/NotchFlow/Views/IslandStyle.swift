import SwiftUI

enum IslandStyle {
    static let primaryText = Color.white
    static let secondaryText = Color.white.opacity(0.58)
    static let tertiaryText = Color.white.opacity(0.38)
    static let accentText = Color.white.opacity(0.85)
    static let surfaceFill = Color.white.opacity(0.07)
    static let surfaceStroke = Color.white.opacity(0.1)
}
